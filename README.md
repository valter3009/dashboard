# Project Management System

Полнофункциональная система управления проектами (аналог Trello/Jira) с канбан-досками, задачами, временными треками, диаграммами Ганта и инструментами для командной работы.

## 🚀 Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy 2.0+** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **Alembic** - миграции базы данных
- **JWT** - аутентификация с токенами
- **Pydantic v2** - валидация данных

### Frontend (в разработке)
- **React 18+** с TypeScript
- **Tailwind CSS** + shadcn/ui
- **Zustand** - управление состоянием
- **@dnd-kit** - drag & drop
- **React Router v6**

### DevOps
- **Docker** - контейнеризация
- **Railway** - платформа для деплоя
- **PostgreSQL** - база данных

## 📋 Основные возможности

### ✅ Реализовано

#### Аутентификация и пользователи
- ✅ Регистрация пользователей
- ✅ Вход по email/password
- ✅ JWT токены (access + refresh)
- ✅ Профиль пользователя
- ✅ Список пользователей

#### Организации
- ✅ Создание организаций
- ✅ Управление участниками
- ✅ Роли (Owner, Admin, Member, Viewer)

#### База данных
- ✅ Полная схема БД (21 таблица)
- ✅ Модели SQLAlchemy для всех сущностей
- ✅ Индексы для производительности
- ✅ Связи между таблицами

#### API
- ✅ REST API эндпоинты
- ✅ Автоматическая документация (Swagger/OpenAPI)
- ✅ Валидация с Pydantic
- ✅ CORS middleware

### 🚧 В разработке

- Проекты (создание, управление, участники)
- Канбан доски с drag & drop
- Задачи (CRUD, назначение, метки, приоритеты)
- Комментарии и вложения
- Чек-листы
- Учет времени (таймер, отчеты)
- Диаграммы Ганта
- Аналитика (velocity, burndown, completion)
- Уведомления
- WebSocket для real-time обновлений
- React frontend

## 🛠 Установка и запуск

### Требования
- Docker и Docker Compose
- Python 3.11+ (для локальной разработки)
- Node.js 18+ (для frontend)

### Быстрый старт с Docker

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd dashboard
```

2. Запустите все сервисы:
```bash
docker-compose up -d
```

3. Backend будет доступен по адресу:
- API: http://localhost:8000
- Документация: http://localhost:8000/api/docs
- База данных: localhost:5432

### Локальная разработка (без Docker)

#### Backend

1. Создайте виртуальное окружение:
```bash
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Создайте файл .env:
```bash
cp .env.example .env
# Отредактируйте .env с вашими настройками
```

4. Запустите PostgreSQL (или используйте Docker):
```bash
docker run -d \
  --name postgres \
  -e POSTGRES_USER=pmuser \
  -e POSTGRES_PASSWORD=pmpassword \
  -e POSTGRES_DB=project_management \
  -p 5432:5432 \
  postgres:15-alpine
```

5. Примените миграции:
```bash
alembic upgrade head
```

6. Запустите сервер:
```bash
uvicorn app.main:app --reload
```

## 📚 API Документация

После запуска backend, документация доступна по адресам:
- **Swagger UI**: http://localhost:8000/api/docs
- **ReDoc**: http://localhost:8000/api/redoc
- **OpenAPI JSON**: http://localhost:8000/api/openapi.json

### Основные эндпоинты

#### Аутентификация
```
POST   /api/v1/auth/register      - Регистрация
POST   /api/v1/auth/login         - Вход
POST   /api/v1/auth/refresh       - Обновление токена
POST   /api/v1/auth/logout        - Выход
```

#### Пользователи
```
GET    /api/v1/users/me           - Текущий пользователь
PUT    /api/v1/users/me           - Обновить профиль
GET    /api/v1/users/{id}         - Получить пользователя
GET    /api/v1/users              - Список пользователей
```

#### Организации
```
POST   /api/v1/organizations              - Создать организацию
GET    /api/v1/organizations              - Список организаций
GET    /api/v1/organizations/{id}         - Получить организацию
PUT    /api/v1/organizations/{id}         - Обновить организацию
DELETE /api/v1/organizations/{id}         - Удалить организацию
POST   /api/v1/organizations/{id}/members - Добавить участника
GET    /api/v1/organizations/{id}/members - Список участников
```

## 🗄 Структура базы данных

### Основные таблицы

- **users** - пользователи системы
- **organizations** - организации
- **organization_members** - участники организаций
- **projects** - проекты
- **project_members** - участники проектов
- **boards** - канбан доски
- **columns** - колонки на досках
- **tasks** - задачи
- **task_assignees** - исполнители задач
- **task_dependencies** - зависимости между задачами
- **labels** - метки для задач
- **comments** - комментарии к задачам
- **attachments** - вложения
- **checklists** - чек-листы
- **checklist_items** - элементы чек-листов
- **time_entries** - записи времени
- **notifications** - уведомления
- **activity_log** - журнал активности
- **custom_fields** - кастомные поля
- **task_custom_field_values** - значения кастомных полей

## 🚀 Деплой на Railway

### Подготовка

1. Создайте аккаунт на [railway.app](https://railway.app)
2. Установите Railway CLI (опционально)

### Деплой

1. Подключите репозиторий к Railway
2. Добавьте PostgreSQL сервис
3. Настройте environment variables:
   - `DATABASE_URL` - автоматически из PostgreSQL сервиса
   - `JWT_SECRET_KEY` - сгенерируйте secure ключ
   - `ALLOWED_ORIGINS` - URL вашего frontend
   - `DEBUG` - установите в `False` для production

4. Railway автоматически:
   - Обнаружит Dockerfile
   - Соберет образ
   - Запустит контейнер
   - Применит миграции при старте

## 📁 Структура проекта

```
dashboard/
├── backend/                    # Backend приложение
│   ├── app/
│   │   ├── api/               # API endpoints
│   │   │   └── v1/           # API версии 1
│   │   ├── models/            # SQLAlchemy модели
│   │   ├── schemas/           # Pydantic схемы
│   │   ├── services/          # Бизнес-логика
│   │   ├── utils/             # Утилиты
│   │   ├── middleware/        # Middleware
│   │   ├── config.py          # Конфигурация
│   │   ├── database.py        # БД подключение
│   │   ├── dependencies.py    # FastAPI зависимости
│   │   └── main.py            # Точка входа
│   ├── alembic/              # Миграции БД
│   ├── tests/                # Тесты
│   ├── Dockerfile            # Docker конфигурация
│   ├── requirements.txt      # Python зависимости
│   └── alembic.ini           # Alembic конфигурация
├── frontend/                  # Frontend приложение (в разработке)
│   ├── src/
│   │   ├── components/       # React компоненты
│   │   ├── pages/            # Страницы
│   │   ├── store/            # Состояние
│   │   ├── api/              # API клиент
│   │   └── hooks/            # Custom hooks
│   └── package.json
├── docker-compose.yml         # Docker Compose конфигурация
├── railway.json              # Railway конфигурация
└── README.md                 # Документация
```

## 🧪 Тестирование

```bash
cd backend
pytest
pytest --cov=app tests/  # С покрытием кода
```

## 📝 Разработка

### Создание новой миграции

```bash
cd backend
alembic revision --autogenerate -m "Description of changes"
alembic upgrade head
```

### Форматирование кода

```bash
black app/
ruff check app/
mypy app/
```

## 🤝 Участие в разработке

Проект находится в активной разработке. Приветствуются:
- Pull requests
- Сообщения об ошибках
- Предложения по улучшению

## 📜 Лицензия

MIT License

## 👥 Авторы

Developed with Claude Code

## 🔮 Планы развития

### Приоритетные задачи:
1. ✅ Базовая структура и аутентификация
2. 🚧 Frontend React приложение
3. 🚧 Проекты и участники
4. 🚧 Канбан доски с drag & drop
5. 🚧 Задачи и комментарии
6. 📋 Учет времени
7. 📋 Диаграммы Ганта
8. 📋 Аналитика и отчеты
9. 📋 Real-time обновления
10. 📋 Уведомления

---

**Статус проекта**: 🚧 В активной разработке
**Версия**: 0.1.0
**Дата обновления**: 2025-11-16