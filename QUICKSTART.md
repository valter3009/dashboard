# 🚀 Быстрый старт - Project Management System

Полная инструкция по запуску проекта локально и деплою на Railway.

---

## 📋 Содержание

1. [Локальный запуск (без Docker)](#локальный-запуск-без-docker)
2. [Локальный запуск (с Docker)](#локальный-запуск-с-docker)
3. [Деплой на Railway](#деплой-на-railway)
4. [Первые шаги после запуска](#первые-шаги-после-запуска)

---

## 🖥️ Локальный запуск (без Docker)

### Требования
- Python 3.11+
- Node.js 18+
- PostgreSQL 15+ (опционально, можно использовать SQLite)

### 1. Запуск Backend

```bash
# Перейдите в директорию backend
cd backend

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте его
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Создайте файл .env из примера
cp .env.example .env

# Отредактируйте .env файл (можно оставить как есть для SQLite)
# DATABASE_URL=sqlite:///./test.db  # Для SQLite
# или
# DATABASE_URL=postgresql://user:password@localhost:5432/project_management  # Для PostgreSQL

# Примените миграции базы данных
alembic upgrade head

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

✅ Backend запущен на: **http://localhost:8000**
📚 API документация: **http://localhost:8000/api/docs**

### 2. Запуск Frontend

Откройте **новый терминал**:

```bash
# Перейдите в директорию frontend
cd frontend

# Установите зависимости
npm install

# Создайте .env файл
cp .env.example .env

# Запустите dev-сервер
npm run dev
```

✅ Frontend запущен на: **http://localhost:3000**

### 3. Готово! 🎉

Откройте браузер и перейдите на **http://localhost:3000**

---

## 🐳 Локальный запуск (с Docker)

**Самый простой способ!**

### Требования
- Docker Desktop установлен и запущен
- Docker Compose

### Запуск всего приложения одной командой

```bash
# В корневой директории проекта
docker-compose up -d
```

Эта команда запустит:
- ✅ PostgreSQL базу данных
- ✅ Backend API сервер
- ✅ Frontend веб-приложение

### Проверка статуса

```bash
# Проверить запущенные контейнеры
docker-compose ps

# Посмотреть логи
docker-compose logs -f

# Посмотреть логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend
```

### Остановка

```bash
# Остановить все сервисы
docker-compose down

# Остановить и удалить volumes (очистить базу данных)
docker-compose down -v
```

✅ Frontend: **http://localhost:3000**
✅ Backend API: **http://localhost:8000**
📚 API документация: **http://localhost:8000/api/docs**

---

## ☁️ Деплой на Railway

### 1. Подготовка

#### Создайте аккаунт
1. Перейдите на https://railway.app
2. Зарегистрируйтесь (можно через GitHub)

### 2. Деплой через GitHub (рекомендуется)

#### Шаг 1: Подключите репозиторий
1. Запушьте код на GitHub
2. В Railway нажмите "New Project"
3. Выберите "Deploy from GitHub repo"
4. Выберите ваш репозиторий

#### Шаг 2: Добавьте PostgreSQL
1. В проекте нажмите "+ New"
2. Выберите "Database" → "PostgreSQL"
3. Railway автоматически создаст базу данных
4. Скопируйте `DATABASE_URL` из переменных окружения

#### Шаг 3: Настройте Backend

В настройках backend сервиса добавьте переменные окружения:

```env
DATABASE_URL=${{Postgres.DATABASE_URL}}
JWT_SECRET_KEY=your-super-secret-production-key-min-32-chars
DEBUG=False
ALLOWED_ORIGINS=https://your-frontend.up.railway.app
SECRET_KEY=your-production-secret-key
BACKEND_URL=https://your-backend.up.railway.app
FRONTEND_URL=https://your-frontend.up.railway.app
```

**Важно!**
- Замените `your-frontend.up.railway.app` на реальный URL вашего frontend
- Сгенерируйте безопасные ключи для `JWT_SECRET_KEY` и `SECRET_KEY`

#### Шаг 4: Настройте Frontend

В настройках frontend сервиса:

```env
VITE_API_URL=https://your-backend.up.railway.app
```

#### Шаг 5: Deploy!

Railway автоматически:
- Обнаружит Dockerfile
- Соберет образы
- Запустит контейнеры
- Применит миграции
- Назначит публичные URL

### 3. Деплой через Railway CLI (альтернатива)

```bash
# Установите Railway CLI
npm install -g @railway/cli

# Войдите в аккаунт
railway login

# Инициализируйте проект
railway init

# Добавьте PostgreSQL
railway add --database postgres

# Deploy backend
railway up --service backend

# Deploy frontend
railway up --service frontend
```

### 4. Проверка деплоя

1. В Railway Dashboard откройте ваш проект
2. Вы увидите три сервиса:
   - 🗄️ PostgreSQL
   - ⚙️ Backend
   - 🌐 Frontend
3. Нажмите на Frontend → "Settings" → "Generate Domain"
4. Откройте сгенерированный URL

✅ **Готово!** Ваше приложение доступно в интернете!

---

## 🎯 Первые шаги после запуска

### 1. Регистрация

1. Откройте приложение в браузере
2. Нажмите "Sign up"
3. Заполните форму регистрации:
   - Email: ваш email
   - Username: ваш логин
   - Password: минимум 8 символов
   - First/Last name: ваше имя (опционально)
4. Нажмите "Create account"

### 2. Вход в систему

1. После регистрации вы будете перенаправлены на страницу входа
2. Введите email и пароль
3. Нажмите "Sign in"

### 3. Создание первого проекта

1. В меню слева выберите "Projects"
2. Нажмите кнопку "+ New Project"
3. Заполните форму:
   - **Project Name**: название проекта (например, "My First Project")
   - **Project Key**: короткий код (например, "MFP")
   - **Description**: описание проекта (опционально)
4. Нажмите "Create Project"

### 4. Что дальше?

Сейчас доступны базовые функции:
- ✅ Регистрация и аутентификация
- ✅ Dashboard с метриками
- ✅ Создание и просмотр проектов
- ✅ REST API с документацией

**В разработке:**
- 📋 Канбан доски
- ✏️ Управление задачами
- 👥 Команды и участники
- ⏱️ Учет времени
- 📊 Диаграмма Ганта
- 📈 Аналитика

---

## 🔧 Полезные команды

### Backend

```bash
# Создать новую миграцию
alembic revision --autogenerate -m "Description"

# Применить миграции
alembic upgrade head

# Откатить последнюю миграцию
alembic downgrade -1

# Запустить тесты
pytest

# Проверить код
black app/
ruff check app/
mypy app/
```

### Frontend

```bash
# Запустить dev-сервер
npm run dev

# Собрать для production
npm run build

# Предпросмотр production сборки
npm run preview

# Проверить код
npm run lint
```

### Docker

```bash
# Пересобрать образы
docker-compose build

# Пересобрать и запустить
docker-compose up -d --build

# Посмотреть логи
docker-compose logs -f [service-name]

# Выполнить команду в контейнере
docker-compose exec backend bash
docker-compose exec frontend sh

# Очистить всё
docker-compose down -v --rmi all
```

---

## 🐛 Решение проблем

### Backend не запускается

**Проблема:** `ModuleNotFoundError: No module named 'app'`

**Решение:**
```bash
# Убедитесь что вы в директории backend
cd backend

# Убедитесь что виртуальное окружение активировано
source venv/bin/activate

# Переустановите зависимости
pip install -r requirements.txt
```

### Frontend не подключается к Backend

**Проблема:** `Network Error` или `CORS policy`

**Решение:**
1. Проверьте что backend запущен на `http://localhost:8000`
2. Проверьте `.env` файл в `frontend/`:
   ```
   VITE_API_URL=http://localhost:8000
   ```
3. Перезапустите frontend: `npm run dev`

### База данных не создается

**Проблема:** `could not connect to database`

**Решение для SQLite:**
```bash
# Удалите старую БД
rm test.db

# Создайте заново
alembic upgrade head
```

**Решение для PostgreSQL:**
```bash
# Убедитесь что PostgreSQL запущен
docker-compose up postgres -d

# Проверьте подключение
docker-compose exec postgres psql -U pmuser -d project_management
```

### Docker контейнеры не запускаются

**Проблема:** `port is already allocated`

**Решение:**
```bash
# Остановите конфликтующие процессы
# Для порта 8000 (backend)
lsof -ti:8000 | xargs kill -9

# Для порта 3000 (frontend)
lsof -ti:3000 | xargs kill -9

# Или измените порты в docker-compose.yml
```

---

## 📚 Дополнительная информация

### Структура проекта

```
dashboard/
├── backend/          # FastAPI backend
│   ├── app/         # Код приложения
│   ├── alembic/     # Миграции БД
│   └── tests/       # Тесты
├── frontend/        # React frontend
│   ├── src/         # Исходный код
│   └── public/      # Статические файлы
└── docker-compose.yml
```

### Технологии

**Backend:**
- FastAPI (Python 3.11)
- SQLAlchemy 2.0
- PostgreSQL / SQLite
- JWT аутентификация
- Pydantic валидация

**Frontend:**
- React 18
- TypeScript
- Tailwind CSS
- React Router
- Zustand (state management)
- React Query (data fetching)

### Полезные ссылки

- [FastAPI документация](https://fastapi.tiangolo.com/)
- [React документация](https://react.dev/)
- [Railway документация](https://docs.railway.app/)
- [Tailwind CSS](https://tailwindcss.com/)

---

## 💬 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел "Решение проблем" выше
2. Посмотрите логи: `docker-compose logs -f`
3. Создайте Issue в GitHub репозитории

---

**Удачи с вашим проектом!** 🚀
